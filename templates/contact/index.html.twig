{% extends 'base.html.twig' %} {% block title %}Contact - TechBlog{% endblock %}
{% block body %}
<div class="row">
    <div class="col-lg-8 mx-auto">
        <h1 class="mb-4">- Nous contacter -</h1>

        <div class="card shadow p-3">
            {{ form_start(form) }}
            {{ form_row(form.nom) }}
            {{ form_row(form.email) }}
            {{ form_row(form.message) }}
            <button type="submit" class="btn btn-primary">📤 Envoyer</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
