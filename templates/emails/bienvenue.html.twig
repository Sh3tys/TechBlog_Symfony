<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <style>
            body {
                font-family: Arial, sans-serif;
                background: #f5f5f5;
                margin: 0;
                padding: 0;
                color: #333;
            }
            .container {
                max-width: 600px;
                margin: 20px auto;
                background: #fff;
                padding: 20px;
            }
            h1,
            h2 {
                margin-top: 0;
            }
            .info-box {
                background: #f0f0f0;
                padding: 10px;
                margin: 15px 0;
                font-size: 14px;
            }
            .button {
                display: inline-block;
                padding: 10px 15px;
                background: #0d6efd;
                color: #fff;
                text-decoration: none;
                margin-top: 15px;
            }
            .footer {
                margin-top: 30px;
                font-size: 13px;
                color: #666;
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>Bienvenue sur TechBlog</h1>

            <p>Bonjour {{ utilisateur.pseudo }},</p>

            <p>Votre compte a été créé avec succès.</p>

            <div class="info-box">
                Email : {{ utilisateur.email }}<br />
                Pseudo : {{ utilisateur.pseudo }}<br />
                Inscription :
                {{ utilisateur.dateInscription|date('d/m/Y H:i') }}
            </div>

            <p>
                Vous pouvez maintenant consulter les articles et gérer votre
                profil.
            </p>

            <a href="{{ url('app_accueil') }}" class="button"
                >Accéder au site</a
            >

            <div class="footer">© {{ "now"|date("Y") }} TechBlog</div>
        </div>
    </body>
</html>
